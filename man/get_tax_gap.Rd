% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_tax_gap.R
\name{get_tax_gap}
\alias{get_tax_gap}
\title{Download HMRC tax gap estimates}
\source{
\url{https://www.gov.uk/government/statistics/measuring-tax-gaps}
}
\usage{
get_tax_gap(tax = NULL, cache = TRUE)
}
\arguments{
\item{tax}{Character vector or \code{NULL} (default = all taxes). Filter by
tax type, e.g. \code{"Income Tax"}, \code{"VAT"}, \code{"Corporation Tax"}. Use
\code{unique(get_tax_gap()$tax)} to see all available values.}

\item{cache}{Logical. Use cached file if available (default \code{TRUE}).}
}
\value{
A data frame with columns:
\describe{
\item{tax_year}{Character. Financial year of the estimate,
e.g. \code{"2023-24"}.}
\item{tax}{Character. Tax type.}
\item{taxpayer_type}{Character. Taxpayer group (e.g. \code{"Individuals"},
\code{"Small businesses"}).}
\item{component}{Character. Behaviour component (e.g. \code{"Evasion"},
\code{"Error"}, \code{"Avoidance"}).}
\item{gap_pct}{Numeric. Tax gap as a percentage of the theoretical
tax liability. \code{NA} where not disclosed.}
\item{gap_gbp_bn}{Numeric. Absolute tax gap in billions of pounds.}
\item{uncertainty}{Character. HMRC uncertainty rating for the
estimate (e.g. \code{"Low"}, \code{"Medium"}, \code{"High"}).}
}
}
\description{
Downloads and tidies the HMRC Measuring the Tax Gap publication, which
estimates the difference between the tax theoretically owed and the tax
actually collected, broken down by tax type and taxpayer group. Published
annually in June, covering the most recent financial year.
}
\details{
The tax gap publication is cross-sectional: each edition covers a single
financial year. This function returns data for the most recent edition
available on GOV.UK. Historical estimates back to 2005-06 are available
in a separate HMRC publication (Measuring the Tax Gap: Time Series).
}
\examples{
\donttest{
# Full tax gap breakdown
get_tax_gap()

# VAT gap only
get_tax_gap(tax = "VAT")
}

}
