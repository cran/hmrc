% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_property_transactions.R
\name{get_property_transactions}
\alias{get_property_transactions}
\title{Download monthly UK property transaction counts}
\source{
\url{https://www.gov.uk/government/statistics/monthly-property-transactions-completed-in-the-uk-with-value-40000-or-above}
}
\usage{
get_property_transactions(
  type = c("all", "residential", "non_residential"),
  nation = NULL,
  start = NULL,
  end = NULL,
  cache = TRUE
)
}
\arguments{
\item{type}{Character. One of \code{"all"} (default), \code{"residential"}, or
\code{"non_residential"}.}

\item{nation}{Character vector or \code{NULL} (default = all nations).
Valid values: \code{"uk"}, \code{"england"}, \code{"scotland"}, \code{"wales"},
\code{"northern_ireland"}.}

\item{start}{Character \code{"YYYY-MM"} or a \code{Date} object. Rows before this
month are dropped.}

\item{end}{Character \code{"YYYY-MM"} or a \code{Date} object. Rows after this
month are dropped.}

\item{cache}{Logical. If \code{TRUE} (default), the downloaded file is cached.}
}
\value{
A data frame with columns:
\describe{
\item{date}{\code{Date}. The first day of the reference month.}
\item{nation}{Character. One of \code{"uk"}, \code{"england"}, \code{"scotland"},
\code{"wales"}, or \code{"northern_ireland"}.}
\item{type}{Character. \code{"residential"} or \code{"non_residential"}.}
\item{transactions}{Numeric. Number of transactions.}
}
}
\description{
Downloads and tidies the HMRC Monthly Property Transactions bulletin,
which counts residential and non-residential property transactions
(SDLT returns, LBTT in Scotland, LTT in Wales) for England, Scotland,
Wales, Northern Ireland, and the UK total. Data runs from April 2005
to the most recent completed month.
}
\examples{
\donttest{
# All nations, all types
get_property_transactions()

# Residential only, England, since 2020
get_property_transactions(type = "residential", nation = "england",
                          start = "2020-01")
}

}
