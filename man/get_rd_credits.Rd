% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_rd_credits.R
\name{get_rd_credits}
\alias{get_rd_credits}
\title{Download HMRC R&D tax credit statistics}
\source{
\url{https://www.gov.uk/government/statistics/corporate-tax-research-and-development-tax-credit}
}
\usage{
get_rd_credits(scheme = NULL, measure = NULL, cache = TRUE)
}
\arguments{
\item{scheme}{Character vector or \code{NULL} (default = all schemes).
Valid values: \code{"sme"} (SME R&D Relief), \code{"rdec"} (Research and
Development Expenditure Credit / large company scheme), \code{"total"}
(combined).}

\item{measure}{Character vector or \code{NULL} (default = all measures).
Valid values: \code{"claims"} (number of claims), \code{"amount_gbp_m"} (cost
in millions of pounds).}

\item{cache}{Logical. Use cached file if available (default \code{TRUE}).}
}
\value{
A data frame with columns:
\describe{
\item{tax_year}{Character. Financial year, e.g. \code{"2023-24"}.}
\item{scheme}{Character. Scheme identifier.}
\item{description}{Character. Plain-English scheme label.}
\item{measure}{Character. Either \code{"claims"} or \code{"amount_gbp_m"}.}
\item{value}{Numeric. Number of claims or cost in millions of pounds.}
}
}
\description{
Downloads and tidies the HMRC Research and Development Tax Credits
Statistics publication, covering the SME R&D Relief and Research and
Development Expenditure Credit (RDEC) schemes. Annual data runs from
2000-01 to the most recent financial year, published annually in September.
}
\details{
Data before 2003-04 covers only the SME scheme (RDEC / large company
scheme was introduced in 2002). Figures for the most recent two years
are provisional and subject to revision as late claims are processed.
}
\examples{
\donttest{
# All R&D credit data
get_rd_credits()

# SME scheme claims only
get_rd_credits(scheme = "sme", measure = "claims")
}

}
